# FirstPass EDI — Python dependencies
# Install with: pip install -r requirements.txt

# LLM + AI
openai>=1.30.0                 # OpenAI chat completions (Validation & Diagnostic agents)
langchain-core>=0.2.0          # Prompt templates and output parsers

# Vector store (local demo — swap for Pinecone / Weaviate in production)
chromadb>=0.5.0

# Data / utilities
python-dotenv>=1.0.0           # .env file loading
pydantic>=2.0.0                # Data validation (used by chromadb)

# Dev / testing (optional)
pytest>=8.0.0
rich>=13.0.0                   # Pretty console output for demo runs
